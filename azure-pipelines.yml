trigger: 
 - master

pool:
   vmImage: 'ubuntu-latest'

jobs:
- job: init
  displayName: init
  steps:
  - bash: |
      #echo '- script: echo Can I?' > outfile.txt
      #echo '- script: echo Yes, I can!' >> outfile.txt
      echo '##vso[task.setvariable variable=code;isOutput=true][{script: echo "Can I?"}, {script: echo "Yes, I can!"}]'
    name: setup

- job: execute
  displayName: execute
  dependsOn: init
  variables:
    myCode: $[ dependencies.init.outputs['setup.code'] ] 
  steps:
  - template: templ.yml
    parameters:
      myCode: 
        - bash: echo Test #Passes
          displayName: Test - Will Pass

