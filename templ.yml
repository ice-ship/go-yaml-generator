parameters:
- name: 'myCode'  
  type: stepList
  default: []

steps:
- bash: |
    echo ${{ parameters.myCode }}
  name: execute
- ${{ each step in parameters.myCode }}:
  - ${{ each pair in step }}:
    ${{ pair.key }}: ${{ pair.value }}       
          